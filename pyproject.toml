[project]
name = "equatorial"
version = "0.1.0"
description = "Equatorial transport-climate-agriculture data tooling for full-year equatorial belt fetch, overlay, and crop accessibility analysis."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cdsapi",
    "duckdb",
    "geopandas",
    "gcsfs",
    "geoalchemy2",
    "httpx",
    "jupyter",
    "matplotlib",
    "notebook",
    "openpyxl",
    "pandas",
    "pyarrow",
    "pycountry",
    "psycopg[binary]",
    "pyyaml",
    "rasterio",
    "shapely",
    "sqlalchemy",
    "xarray",
    "zarr",
]

[project.scripts]
equatorial-fetch-data = "src.data.fetch:main"
equatorial-inspect-data = "src.data.inspect:main"
equatorial-render-country-previews = "src.data.render_country_previews:main"
equatorial-list-equator-countries = "src.data.list_equatorial_countries:main"
equatorial-road-hazard-overnight = "src.data.run_road_hazard_overnight_worker:main"
equatorial-select-crop-origins = "src.data.select_baseline_connected_crop_origins:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src*"]
